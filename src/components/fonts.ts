import { Inter, Roboto_Condensed, Oswald } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const roboto_condensed = Roboto_Condensed({ preload: false });

export const oswald = Oswald({ subsets: ["latin"] });
